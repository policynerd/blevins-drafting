Files were lifted from `policynerd/blevins-docket-mgr` (`apps/`, `packages/`,
root drafting Docker/Fly/turbo config). Brand lockup PNGs under
`packages/pdf/src/assets/` remain in the docket repo until they are copied
here as binaries.
